;Definitions given by the exercise.
(define prefix "hello")
(define suffix "world")

;Expression that adds an underscore between two 1Strings.
(define underscore-add
  (string-append (string-append prefix "_") suffix))
